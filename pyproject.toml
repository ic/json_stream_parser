[tool.poetry]
name = "json_stream_parser"
version = "0.1.0"
description = ""
authors = ["Not Sure <not@sure.tm>"]

[tool.poetry.dependencies]
python = "^3.5"

[tool.poetry.dev-dependencies]
pytest = "^5.4.2"

[build-system]
requires = ["poetry>=0.12"]
build-backend = "poetry.masonry.api"
